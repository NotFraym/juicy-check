services:
  juicy-check:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8010:5000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production